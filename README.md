# ReadMe – Infraestructura

Docker Compose para desplegar la plataforma ReadMe en dos VMs:

- `docker-compose.dbs.yml` — bases de datos (PostgreSQL, MySQL, MongoDB) para la VM de BDs
- `docker-compose.yml` — los 4 microservicios para la VM de servicios
